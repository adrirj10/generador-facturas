{
    "buildCommand": "npm install && npm run build",
    "outputDirectory": "dist/invoice-generator",
    "framework": "angular",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }